<%= link_to "New Goal" , new_goal_path %>

<h1 id="my-goals"> My Goals</h1>
<% @goals.each do |goal| %>
	<div class="goal-box">
		<h2><%= goal.title %></h2>
		<p><%= goal.description %></p>
		<%= link_to "Show Goal", goal_path(goal.id) %>
	</div>
<% end %>

