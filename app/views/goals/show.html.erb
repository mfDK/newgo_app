<div class="container">
		<h1 id="goal-title"><%= @goal.title %></h1>

		<%= link_to "Edit Goal" , edit_goal_path(@goal) , :class => "btn btn-info" %>

		<div>
			  <%= tasks_left %>
		</div>
		
		<div class="g-desc">
			<h3 id="g-desc-title">Description: </h3>
			<%= @goal.description %>
		</div>
		<div class="g-end">
			<h3 id="end-date">End Date: <%= @goal.end_date.to_date.strftime("%m/%d/%Y") %></h3>
			<%= link_to "New Task" , new_goal_task_path(@goal) , :class => "btn btn-primary" %>
		</div>

		
		<%= render @tasks %>
		<!-- This iterates through each task for a goal, this is in the goals_controller -->
		<!-- the view for this is a partial _tasks.html.erb -->


		<!-- <%= @post_array %>
		<%= @post_array_exist %>
		<%= @fail_post_exist %> -->
</div>